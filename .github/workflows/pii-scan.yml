name: PII & Secrets Scan

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  gitleaks:
    uses: fitz123/claude-code-bot/.github/workflows/gitleaks-reusable.yml@gitleaks-scan-commit-messages
    secrets:
      CONFIG_PAT: ${{ secrets.GITLEAKS_CONFIG_PAT }}
